"use client";
import React from "react";

const AddToCart = () => {
    return (
        <button className="btn btn-soft" onClick={() => console.log("Added to cart")}>
            Add to Cart
        </button>
    );
};

export default AddToCart;
